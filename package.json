{
  "name": "roller-rpc-client",
  "version": "0.0.1",
  "description": "Azimuth Rollup RPC-API Client",
  "scripts": {
    "generate-clients": "open-rpc-generator generate -c open-rpc-generator-config.json",
    "clean": "rm -rf build && mkdir -p build",
    "build": "npm run clean && npm run generate-clients && npm test",
    "test": "jest"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@open-rpc/generator": "^1.20.0",
    "@open-rpc/meta-schema": "^1.14.2",
    "@open-rpc/schema-utils-js": "^1.15.0",
    "@types/jest": "26.0.24",
    "jest": "27.0.6",
    "ts-jest": "27.0.3",
    "tslint": "6.1.3",
    "typescript": "4.3.5"
  }
}
